[build]
  publish = "dist"
  command = "npm run build"

[functions]
  # zisi = default bundler; esbuild can cause 502 in some Lambda envs
  node_bundler = "zisi"

[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
