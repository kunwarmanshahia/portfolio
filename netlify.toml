[build]
  publish = "dist"
  command = "npm run build"

[functions]
  node_bundler = "esbuild"
  # used by lib/portfolioContext.js at runtime
  included_files = ["pages/**/*.tsx"]

[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
