# Copy this to .env and add your key. Do not commit .env.
GEMINI_API_KEY=your_gemini_api_key_here

# If you get 429 "limit: 0": in Google Cloud, attach billing to the project
# your API key belongs to (free tier still applies). Then check ai.dev/rate-limit
# and regenerate a new API key; keys created before billing can stay at 0 quota.
